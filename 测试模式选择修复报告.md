# 测试模式选择修复报告

## 修复时间
2026年5月7日

## 问题根源

用户指出了一个关键问题：
> "为啥两个版本都是test router？它总的界面也是test，其他的题目页面也是，这个工具还有啥用是吧"

**根本原因**：
1. 六问版和完整版都在同一个 `/test` 路由
2. 它们通过 `testMode` 状态区分（`'quick'` 或 `'deep'`）
3. AI之前调用 `navigate` 跳转到 `/test`，但没有设置 `testMode`
4. 导致跳转了但没有选择测试模式，测试无法开始

## 应用结构分析

### 路由配置
```javascript
// router/index.js
{
  path: '/test',
  name: 'Test',
  component: () => import('@/views/TestView.vue'),
}
```

### TestView组件
```javascript
// TestView.vue
const testMode = ref(userStore.testMode || '')  // 'quick' 或 'deep'

const currentQuestions = computed(() => {
  if (testMode.value === 'deep') return deepTestQuestions  // 完整版题目
  if (testMode.value === 'quick') return quickTestQuestions  // 六问版题目
  return []
})

function selectMode(mode) {
  testMode.value = mode
  userStore.setTestMode(mode)
  // ...
}
```

**关键发现**：
- 同一个页面，通过 `testMode` 区分版本
- 需要调用 `userStore.setTestMode(mode)` 来设置模式
- `mode` 可以是 `'quick'`（六问版）或 `'deep'`（完整版）

## 解决方案

### 1. 修改start_guided_test工具，添加mode参数

**后端系统提示词修改**：

```python
'2. start_guided_test - 开始引导测试\n'
'   作用：启动AI语音引导的照心测试，你会逐题播报问题并听取用户答案\n'
'   参数：{mode: "quick" 或 "deep"}  # quick=六问版, deep=完整版\n'
'   前置条件：用户必须已经选择了测试版本（六问版或完整版）\n'
'   何时调用：\n'
'     • 用户说"开始测试"且已选择版本\n'
'     • 如果当前不在测试页，需要先调用 navigate 跳转到 /test 页面\n'
'   ⚠️ 重要：\n'
'     • 如果用户只说"开始测试"没有指定版本，必须先用 ask_clarification 询问版本\n'
'     • mode参数必须是 "quick"（六问版）或 "deep"（完整版）\n'
'     • 调用此工具后，前端会自动播报第一题，你的reply中不要包含题目内容\n'
'     • 你的reply应该只是确认开始，如"好嘞！六问版走起～那咱们就正式开始啦！"\n'
'   示例：\n'
'     • 用户："开始测试" → [{"name": "ask_clarification", "args": {"question": "你想选六问快速版还是完整深度版？"}}]\n'
'     • 用户："六问版" → reply: "好嘞！六问版走起～那咱们就正式开始啦！", tool_calls: [{"name": "start_guided_test", "args": {"mode": "quick"}}]\n'
'     • 用户："完整版" → reply: "好嘞！完整版走起～咱们慢慢聊！", tool_calls: [{"name": "start_guided_test", "args": {"mode": "deep"}}]\n'
```

### 2. 更新示例2

**修改前**：
```json
{
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ],
  "memory": ["用户选择六问版测试"]
}
```

**修改后**：
```json
{
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "start_guided_test", "args": {"mode": "quick"}}
  ],
  "memory": ["用户选择六问版测试"]
}
```

**关键改进**：
- 删除了 `navigate` 工具调用（不需要了）
- `start_guided_test` 现在包含 `mode` 参数
- 更简洁，只需一个工具调用

### 3. 前端实现mode参数处理

**修改位置**：`assistantStore.js` 的 `executeSingleTool` 函数

```javascript
case 'start_test':
case 'start_guided_test':
  console.log(`🎯 启动引导测试`)
  const mode = args.mode || 'quick'  // 默认六问版
  console.log(`   - 测试模式: ${mode} (${mode === 'quick' ? '六问版' : '完整版'})`)
  
  // 设置测试模式
  const userStore = useUserStore()
  userStore.setTestMode(mode)
  console.log(`   - 已设置testMode: ${userStore.testMode}`)
  
  // 激活引导测试
  this.guidedTestActive = true
  this.guidedQuestionIndex = 0
  console.log(`   - guidedTestActive: ${this.guidedTestActive}`)
  console.log(`   - guidedQuestionIndex: ${this.guidedQuestionIndex}`)
  
  // 如果不在测试页，跳转过去
  if (this.stage !== 'test' && this.autoGuide && router) {
    console.log(`   - 当前不在测试页，跳转到 /test`)
    try {
      await router.push('/test')
      console.log(`   - ✅ 已跳转到测试页`)
    } catch (error) {
      console.error(`   - ❌ 跳转失败:`, error)
    }
  } else {
    console.log(`   - 已在测试页，无需跳转`)
  }
  break
```

**关键功能**：
1. 从 `args.mode` 获取测试模式（默认 `'quick'`）
2. 调用 `userStore.setTestMode(mode)` 设置模式
3. 激活引导测试
4. 如果不在测试页，自动跳转
5. 如果已在测试页，无需跳转

## 完整流程

### 场景1：从主页开始测试

```
用户在主页说: "开始测试"
  ↓
AI: "好嘞！咱们有两个版本：六问快速版（5分钟）和完整深度版（15分钟）。你想选哪个？"
tool_calls: [{"name": "ask_clarification", ...}]
  ↓
用户: "六问快速版"
  ↓
AI: "好嘞！六问版走起～那咱们就正式开始啦！"
tool_calls: [{"name": "start_guided_test", "args": {"mode": "quick"}}]
  ↓
前端执行:
  1. userStore.setTestMode('quick')  // 设置六问版
  2. guidedTestActive = true  // 激活引导测试
  3. router.push('/test')  // 跳转到测试页（因为当前在主页）
  4. 等待500ms
  5. 自动播报第一题
```

### 场景2：已在测试页，切换版本

```
用户在测试页说: "开始测试"
  ↓
AI: "好嘞！咱们有两个版本：六问快速版（5分钟）和完整深度版（15分钟）。你想选哪个？"
tool_calls: [{"name": "ask_clarification", ...}]
  ↓
用户: "完整版"
  ↓
AI: "好嘞！完整版走起～咱们慢慢聊！"
tool_calls: [{"name": "start_guided_test", "args": {"mode": "deep"}}]
  ↓
前端执行:
  1. userStore.setTestMode('deep')  // 设置完整版
  2. guidedTestActive = true  // 激活引导测试
  3. 无需跳转（已在测试页）
  4. 等待500ms
  5. 自动播报第一题
```

## 预期日志输出

当用户说"六问快速版"后，浏览器控制台应该显示：

```
================================================================================
📥 前端接收到的数据: {
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "start_guided_test", "args": {"mode": "quick"}}
  ],
  "memory": ["用户选择六问版测试"]
}
================================================================================
✅ 执行 tool_calls: [...]
🎬 开始执行工具调用列表: [...]
📍 当前router对象: {...}
📍 router类型: object
📍 router.push存在: true

📌 准备执行工具 #1: start_guided_test
🔧 执行工具: start_guided_test
   参数: {mode: "quick"}
   router存在: true
   router.push存在: true
🎯 启动引导测试
   - 测试模式: quick (六问版)
   - 已设置testMode: quick
   - guidedTestActive: true
   - guidedQuestionIndex: 0
   - 当前不在测试页，跳转到 /test
   - ✅ 已跳转到测试页
✅ 所有工具执行完毕

🎯 测试刚启动，准备播报第一题
（等待500ms后）
📥 前端接收到的数据: {"reply": "第一题来咯：...", ...}
```

## 修改的文件

1. **e:\Jade\jademirror\backend\jademirror_core\application.py**
   - 更新start_guided_test工具说明，添加mode参数
   - 更新示例2，删除navigate调用，添加mode参数
   - 添加完整版的示例

2. **e:\Jade\jademirror\frontend\src\stores\assistantStore.js**
   - 在executeSingleTool中处理mode参数
   - 调用userStore.setTestMode(mode)设置测试模式
   - 智能判断是否需要跳转（不在测试页才跳转）

3. **e:\Jade\测试模式选择修复报告.md**（本文件）
   - 详细的问题分析和解决方案

## 关键改进

### 1. 简化工具调用
- **之前**：需要两个工具 `navigate` + `start_guided_test`
- **现在**：只需一个工具 `start_guided_test` with `mode`

### 2. 智能跳转
- **之前**：总是调用navigate跳转
- **现在**：只在不在测试页时才跳转

### 3. 明确模式选择
- **之前**：没有设置testMode，测试无法开始
- **现在**：明确设置mode参数（quick或deep）

### 4. 更好的用户体验
- 六问版：`{"mode": "quick"}` → 6道题
- 完整版：`{"mode": "deep"}` → 完整题目

## 测试验证

### 测试1：从主页开始六问版
```
1. 在主页
2. 说"开始测试"
3. 说"六问快速版"
4. 预期：跳转到/test，显示6道题，自动播报第一题
```

### 测试2：从主页开始完整版
```
1. 在主页
2. 说"开始测试"
3. 说"完整深度版"
4. 预期：跳转到/test，显示完整题目，自动播报第一题
```

### 测试3：在测试页切换版本
```
1. 已在测试页
2. 说"开始测试"
3. 说"六问快速版"
4. 预期：不跳转，切换到6道题，自动播报第一题
```

## 总结

这次修复解决了核心问题：
- ✅ 不再需要navigate工具（除非真的要跳转到其他页面）
- ✅ start_guided_test现在包含mode参数，明确指定测试版本
- ✅ 前端正确设置testMode，测试可以正常开始
- ✅ 智能判断是否需要跳转，避免不必要的操作
- ✅ 完整的调试日志，方便排查问题

---

**修复完成时间**：2026年5月7日  
**修复人员**：Kiro AI Assistant  
**测试状态**：待用户测试验证
