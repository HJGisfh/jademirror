# 🔧 工具调用修复说明

## 问题描述

用户说"开始测试"，AI回复正确但不执行操作，没有调用工具跳转页面。

**原因**: 后端返回的是旧格式 `next_action`，但前端期望的是新格式 `tool_calls` 数组。

---

## 修复内容

### 1. 后端修改 ✅

**文件**: `jademirror/backend/jademirror_core/application.py`

**修改位置**: `@bp.post('/api/assistant/turn')` 函数

**修改内容**:
```python
# 新格式：tool_calls 数组
tool_calls = parsed.get('tool_calls')
if tool_calls and isinstance(tool_calls, list):
    # 使用新格式
    pass
else:
    # 兼容旧格式：next_action
    tool_calls = []
    if next_action and next_action != 'free_chat':
        tool_calls.append({
            'name': next_action,
            'args': action_payload
        })

return jsonify(
    {
        'reply': reply,
        'tool_calls': tool_calls,  # 新格式
        'memory_saved': memory_saved,
        'emotion': emotion,
        'memory_digest': digest,
        'privacy_mode': privacy_mode,
    }
)
```

**改进**:
- ✅ 优先使用AI返回的 `tool_calls` 数组（新格式）
- ✅ 如果AI返回的是旧格式 `next_action`，自动转换为 `tool_calls` 数组
- ✅ 向后兼容，支持新旧两种格式

---

## 工作流程

### 场景：用户说"开始测试"

#### 1. 前端发送请求
```javascript
{
  text: "开始测试",
  stage: "home",
  context: {
    tool_definitions: {...},
    available_tools: ["navigate", "start_guided_test", ...],
    ...
  }
}
```

#### 2. 后端AI处理

AI根据系统提示词和上下文，返回：
```json
{
  "reply": "哇，这就来了！走，咱们开始测试~",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ],
  "memory": [],
  "emotion": "excited"
}
```

#### 3. 后端返回给前端
```json
{
  "reply": "哇，这就来了！走，咱们开始测试~",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ],
  "memory_saved": true,
  "emotion": "excited",
  "memory_digest": "...",
  "privacy_mode": false
}
```

#### 4. 前端执行工具
```javascript
// assistantStore.js
for (const call of data.tool_calls) {
  await executeSingleTool(call.name, call.args, router)
}

// 执行结果：
// 1. navigate → 跳转到 /test 页面
// 2. start_guided_test → 开始语音引导测试
```

---

## 测试步骤

### 1. 启动后端
```bash
cd e:\Jade\jademirror\backend
python app.py
```

### 2. 启动前端
```bash
cd e:\Jade\jademirror\frontend
npm run dev
```

### 3. 测试用例

#### 测试1：开始测试
```
用户: "开始测试"
期望: 
- AI回复: "哇，这就来了！走，咱们开始测试~"
- 操作: 跳转到测试页 + 开始语音引导测试
```

#### 测试2：模糊请求
```
用户: "测试"
期望:
- AI回复: "你是想开始照心测试吗？还是想先了解一下测试的内容？"
- 操作: 询问澄清，不跳转
```

#### 测试3：闲聊
```
用户: "为什么古人喜欢玉？"
期望:
- AI回复: 玉文化知识
- 操作: 无，纯聊天
```

---

## 调试方法

### 1. 查看后端日志

在后端添加调试输出：
```python
print(f"AI返回: {output}")
print(f"解析后: {parsed}")
print(f"tool_calls: {tool_calls}")
```

### 2. 查看前端Console

打开浏览器开发者工具（F12），查看：
```javascript
console.log('AI响应:', data)
console.log('tool_calls:', data.tool_calls)
```

### 3. 检查API请求

在Network标签中查看：
- 请求: `/api/assistant/turn`
- 响应: 是否包含 `tool_calls` 数组

---

## 常见问题

### Q1: AI没有返回 tool_calls

**原因**: AI可能返回的是旧格式 `next_action`

**解决**: 后端会自动转换为 `tool_calls` 数组

### Q2: AI返回了 tool_calls 但没有执行

**原因**: 前端可能没有正确解析

**检查**: 
```javascript
// assistantStore.js
if (data.tool_calls && data.tool_calls.length > 0) {
  await this.executeToolCalls(data.tool_calls, router)
}
```

### Q3: 工具调用了但页面没跳转

**原因**: router 可能没有正确传递

**检查**:
```javascript
async executeSingleTool(name, args, router) {
  switch (name) {
    case 'navigate':
      if (args.route && router) {
        router.push(args.route)  // 确保 router 存在
      }
      break
  }
}
```

---

## 总结

✅ **已修复**:
- 后端现在返回 `tool_calls` 数组
- 支持新旧两种格式
- AI可以正确调用工具

✅ **已验证**:
- Python语法检查通过
- 后端可以正常启动

⚠️ **待测试**:
- 实际运行测试用例
- 验证AI决策是否正确
- 验证工具调用是否执行

---

**更新时间**: 2026-05-07  
**版本**: v2.2 - 工具调用修复完成
