# 🚀 玉灵童子快速启动指南

## 第一步：配置环境变量

### 1. 后端配置

复制环境变量模板：
```bash
cd e:\Jade\jademirror\backend
copy .env.example .env
```

编辑 `.env` 文件，填入你的API密钥：

```env
# ===== 必需配置 =====
# DeepSeek API（AI对话）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# 通义千问API（图像生成）
QWEN_API_KEY=sk-your-qwen-api-key-here

# ===== 可选配置 =====
# 端口（默认5000）
PORT=5000

# 是否需要登录（0=不需要，1=需要）
AUTH_REQUIRED=0

# 允许的前端地址
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 如果没有API密钥，可以开启mock模式测试
DEEPSEEK_ALLOW_MOCK=0
```

### 2. 获取API密钥

#### DeepSeek API
1. 访问：https://platform.deepseek.com/
2. 注册账号并登录
3. 进入"API Keys"页面
4. 创建新的API Key
5. 复制密钥到 `.env` 文件

#### 通义千问API
1. 访问：https://dashscope.aliyun.com/
2. 登录阿里云账号
3. 开通DashScope服务
4. 获取API Key
5. 复制密钥到 `.env` 文件

---

## 第二步：安装依赖

### 后端依赖
```bash
cd e:\Jade\jademirror\backend
pip install -r requirements.txt
```

### 前端依赖
```bash
cd e:\Jade\jademirror\frontend
npm install
```

---

## 第三步：启动服务

### 方式1：分别启动（推荐）

**终端1 - 启动后端**：
```bash
cd e:\Jade\jademirror\backend
python app.py
```

看到以下输出表示成功：
```
 * Running on http://0.0.0.0:5000
 * Running on http://127.0.0.1:5000
 * Running on http://192.168.x.x:5000
```

**终端2 - 启动前端**：
```bash
cd e:\Jade\jademirror\frontend
npm run dev
```

看到以下输出表示成功：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

### 方式2：使用启动脚本

创建 `start.bat`（Windows）：
```batch
@echo off
echo 启动玉灵童子...

start "后端服务" cmd /k "cd /d e:\Jade\jademirror\backend && python app.py"
timeout /t 3 /nobreak >nul
start "前端服务" cmd /k "cd /d e:\Jade\jademirror\frontend && npm run dev"

echo 服务启动中...
echo 后端: http://localhost:5000
echo 前端: http://localhost:5173
pause
```

双击运行 `start.bat` 即可。

---

## 第四步：访问应用

1. 打开浏览器（推荐Chrome或Edge）
2. 访问：http://localhost:5173
3. 允许麦克风权限（点击地址栏的麦克风图标）
4. 开始与玉灵童子对话！

---

## 🎤 语音功能测试

### 测试语音输入
1. 点击页面上的**麦克风图标**
2. 说话：**"你好"**
3. 停顿1-2秒
4. 系统会自动识别并显示文字
5. AI会语音回复

### 测试语音输出
- AI的回复会自动语音播报
- 可以在设置中调整语音人格（默认/温暖/明亮/深沉）
- 可以关闭自动播报

---

## 🧪 功能测试清单

### ✅ 基础对话
```
你: "你好"
AI: "嘿！我是玉灵童子..."
```

### ✅ 开始测试
```
你: "开始测试"
AI: "好嘞！我们开始照心测试..."
→ 自动跳转到测试页面
```

### ✅ 玉文化闲聊
```
你: "为什么古人喜欢玉？"
AI: "哈哈这个问题问得好！古人觉得玉有五德..."
→ 纯聊天，不执行操作
```

### ✅ 生成专属玉
```
你: "生成我的玉"
AI: "我这就帮你凝练专属玉意象..."
→ 调用图像生成API
```

### ✅ 展厅管理
```
你: "去展厅看看"
AI: "好的，带你去展厅..."
→ 跳转到展厅页面
```

---

## ⚠️ 常见问题

### Q1: 语音识别不工作
**解决方案**：
1. 检查浏览器是否支持（Chrome/Edge最佳）
2. 确认已允许麦克风权限
3. 检查麦克风是否正常工作
4. 尝试刷新页面

### Q2: AI不回复
**解决方案**：
1. 检查后端是否启动（访问 http://localhost:5000/api/health）
2. 检查 `.env` 文件中的API密钥是否正确
3. 查看浏览器控制台是否有错误
4. 查看后端终端是否有错误日志

### Q3: 图像生成失败
**解决方案**：
1. 确认通义千问API密钥正确
2. 检查API额度是否充足
3. 查看后端日志中的错误信息

### Q4: 跨域错误
**解决方案**：
1. 确认后端 `.env` 中的 `ALLOWED_ORIGINS` 包含前端地址
2. 重启后端服务

---

## 🔍 调试技巧

### 查看后端日志
后端终端会显示所有API请求和错误：
```
INFO:     127.0.0.1:xxxxx - "POST /api/assistant/turn HTTP/1.1" 200 OK
```

### 查看前端日志
打开浏览器开发者工具（F12）：
- **Console**: 查看JavaScript错误
- **Network**: 查看API请求
- **Application**: 查看本地存储

### 测试API健康状态
访问：http://localhost:5000/api/health

应该返回：
```json
{
  "status": "ok",
  "deepseek_configured": true,
  "qwen_configured": true,
  ...
}
```

---

## 📱 移动端访问

### 局域网访问
1. 确保手机和电脑在同一WiFi
2. 查看后端启动时显示的Network地址
3. 在手机浏览器访问：`http://192.168.x.x:5173`

### 注意事项
- 移动端浏览器的语音识别支持可能有限
- iOS Safari需要HTTPS才能使用麦克风
- 建议使用Chrome移动版

---

## 🎯 下一步

### 体验完整流程
1. **照心测试** - 回答10个问题
2. **查看匹配结果** - 看看你匹配到哪块古玉
3. **与古玉对话** - 和你的专属古玉聊天
4. **生成专属玉** - AI生成你的专属玉图像
5. **保存到展厅** - 收藏你的作品
6. **展厅导览** - 语音导览你的收藏

### 探索高级功能
- **长期记忆** - AI会记住你的偏好
- **情感陪伴** - 空闲时AI会主动关怀
- **玉文化闲聊** - 随时和AI聊玉文化
- **多种语音人格** - 在设置中切换

---

## 📞 获取帮助

如果遇到问题：
1. 查看 `玉灵童子修复完成报告.md`
2. 查看后端和前端的日志
3. 检查API配置是否正确
4. 尝试重启服务

---

**祝你使用愉快！🎉**

玉灵童子会一直陪伴你，探索玉文化的奥秘。
