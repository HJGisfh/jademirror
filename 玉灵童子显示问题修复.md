# 玉灵童子显示问题修复

## 问题描述

用户反馈看不到玉灵童子小动物。

## 问题原因

1. **默认状态错误**
   - `assistantStore.open` 默认值为 `true`
   - 导致默认显示展开的面板，而不是小动物
   - 小动物只在 `open: false` 时显示

2. **初始位置可能超出屏幕**
   - 初始位置计算可能在窗口尺寸确定前执行
   - 导致小动物被放到屏幕外

## 修复方案

### 1. 修改默认状态

**文件：** `e:\Jade\jademirror\frontend\src\stores\assistantStore.js`

**修改前：**
```javascript
state: () => ({
  ready: false,
  busy: false,
  open: true,  // ❌ 默认展开
  autoSpeak: true,
  autoGuide: true,
```

**修改后：**
```javascript
state: () => ({
  ready: false,
  busy: false,
  open: false,  // ✅ 默认收起，显示小动物
  autoSpeak: true,
  autoGuide: true,
```

### 2. 优化初始位置设置

**文件：** `e:\Jade\jademirror\frontend\src\components\CompanionPanel.vue`

**修改：**
```javascript
// 初始位置：先设置为0,0，然后在mounted中计算
const position = ref({ x: 0, y: 0 })

onMounted(() => {
  // 延迟设置位置，确保窗口尺寸已确定
  setTimeout(() => {
    position.value = {
      x: window.innerWidth - 100,
      y: window.innerHeight - 100
    }
    console.log('🐾 玉灵童子初始位置:', position.value)
  }, 100)
})
```

### 3. 添加登录状态判断

**确保只有登录用户才能看到小动物：**
```vue
<template>
  <aside 
    v-if="authStore.isLoggedIn"
    ref="companionRef"
    class="companion"
    ...
  >
```

## 预期效果

### 修复后的显示逻辑

1. **未登录用户**
   - 不显示玉灵童子

2. **已登录用户**
   - 默认显示小动物（收起状态）
   - 小动物位于右下角
   - 点击小动物展开面板
   - 点击关闭按钮收起为小动物

### 小动物位置

```
屏幕
┌─────────────────────────────┐
│                             │
│                             │
│                             │
│                             │
│                      ◠   ◠  │
│                     ┌─────┐ │
│                     │ • • │ │ ← 右下角
│                     │  ω  │ │
│                     └─────┘ │
└─────────────────────────────┘
```

## 测试步骤

1. **清除缓存并刷新**
   - 按 Ctrl+Shift+R 强制刷新
   - 或清除浏览器缓存

2. **检查登录状态**
   - 确保已登录
   - 未登录用户看不到小动物

3. **查看小动物**
   - 应该在右下角看到绿色的小动物
   - 小动物有浮动和眨眼动画

4. **测试交互**
   - 点击小动物，应该展开面板
   - 点击关闭按钮，应该收起为小动物
   - 拖动小动物，应该可以移动位置

## 调试方法

### 1. 打开浏览器控制台

按 F12 打开开发者工具，查看控制台输出：

```
🐾 玉灵童子初始位置: {x: 1820, y: 920}
```

### 2. 检查元素

在控制台中运行：

```javascript
// 检查登录状态
console.log('登录状态:', document.querySelector('.companion'))

// 检查open状态
console.log('展开状态:', window.__VUE_DEVTOOLS_GLOBAL_HOOK__)
```

### 3. 检查样式

在Elements标签中查找 `.companion` 元素：
- 检查 `left` 和 `top` 值是否合理
- 检查 `display` 是否为 `none`
- 检查 `opacity` 是否为 `0`

## 常见问题

### Q1: 刷新后还是看不到

**解决方案：**
1. 清除浏览器缓存
2. 按 Ctrl+Shift+R 强制刷新
3. 检查是否已登录

### Q2: 小动物在屏幕外

**解决方案：**
1. 调整浏览器窗口大小
2. 刷新页面，位置会重新计算
3. 或者修改初始位置为固定值：
```javascript
position.value = { x: 50, y: 50 }  // 左上角
```

### Q3: 小动物不显示但面板显示

**原因：** `open: true` 导致默认展开

**解决方案：**
1. 确认 `assistantStore.js` 中 `open: false`
2. 刷新页面

### Q4: 未登录用户也想看到

**修改方案：**
删除 `v-if="authStore.isLoggedIn"` 条件：
```vue
<aside 
  ref="companionRef"
  class="companion"
  ...
>
```

## 相关文件

- `e:\Jade\jademirror\frontend\src\stores\assistantStore.js` - 状态管理
- `e:\Jade\jademirror\frontend\src\components\CompanionPanel.vue` - 组件实现

## 完成时间

2026年5月7日

## 状态

✅ 已修复

## 总结

通过以下修改解决了显示问题：

1. ✅ 将 `open` 默认值改为 `false`，默认显示小动物
2. ✅ 优化初始位置设置，延迟计算确保窗口尺寸已确定
3. ✅ 添加登录状态判断，只有登录用户才能看到
4. ✅ 添加调试日志，方便排查问题

现在小动物应该正常显示在右下角了！
