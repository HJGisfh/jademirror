# 登出清空数据功能

## 修复时间
2026年5月7日

## 需求描述
用户要求：**每次游客登出应该清空所有东西**

## 问题分析

### 登出前可能存在的数据

1. **认证信息**（authStore）
   - currentUser - 当前用户信息
   - token - 认证令牌

2. **用户数据**（userStore）
   - testMode - 测试模式（quick/deep）
   - testAnswers - 测试答案
   - userVector - 用户向量
   - matchedJade - 匹配的古玉
   - matchProfile - 匹配档案
   - matchReason - 匹配原因
   - matchScore - 匹配分数
   - mbtiType - MBTI类型
   - archetype - 原型
   - dimensionScores - 维度分数
   - shadowJade - 阴影玉
   - shadowProfile - 阴影档案
   - flowchartPath - 流程图路径
   - currentEmotion - 当前情绪
   - generatedImageDataUrl - 生成的图片
   - generatedImageOriginalUrl - 原始图片URL
   - generatedModelUrl - 模型URL
   - generatedMultiViews - 多视图
   - lastPrompt - 最后的提示词
   - **works - 展厅作品（存储在localStorage）**

3. **助手数据**（assistantStore）
   - messages - 对话历史
   - memories - 长期记忆
   - lastMemoryDigest - 记忆摘要
   - guidedTestActive - 引导测试状态
   - guidedQuestionIndex - 当前题目索引
   - galleryTourAuto - 展厅自动导览状态
   - 各种定时器

## 解决方案

### 修改authStore的logout方法

**修改位置**：`e:\Jade\jademirror\frontend\src\stores\authStore.js`

**修改内容**：

```javascript
async logout() {
  try {
    if (this.token) {
      await http.post('/auth/logout')
    }
  } catch {
    // ignore logout network errors
  }

  // 清空认证信息
  this.clearAuth()
  this.sessionChecked = true
  
  // 清空用户数据
  const { useUserStore } = await import('./userStore')
  const userStore = useUserStore()
  userStore.resetTest()  // 清空测试答案、匹配结果、生成的图片等
  userStore.works = []  // 清空展厅作品
  userStore.persistWorks()  // 持久化空数组到localStorage
  
  // 清空助手数据
  const { useAssistantStore } = await import('./assistantStore')
  const assistantStore = useAssistantStore()
  assistantStore.messages = []  // 清空对话历史
  assistantStore.memories = []  // 清空记忆
  assistantStore.lastMemoryDigest = ''
  assistantStore.guidedTestActive = false  // 重置测试状态
  assistantStore.guidedQuestionIndex = 0
  assistantStore.galleryTourAuto = false  // 停止展厅导览
  assistantStore.teardown()  // 清理定时器
  
  console.log('✅ 用户登出，所有数据已清空')
}
```

### 清空的数据详细说明

#### 1. 认证信息（已有功能）
```javascript
this.clearAuth()
// 清空：currentUser, token
// 清空localStorage：jademirror-auth-user-v1, jademirror-auth-token-v1
```

#### 2. 用户数据（新增）
```javascript
userStore.resetTest()
// 清空：
// - testMode, testAnswers, userVector
// - matchedJade, matchProfile, matchReason, matchScore
// - mbtiType, archetype, dimensionScores
// - shadowJade, shadowProfile, flowchartPath
// - currentEmotion
// - generatedImageDataUrl, generatedImageOriginalUrl
// - generatedModelUrl, generatedMultiViews, lastPrompt

userStore.works = []
userStore.persistWorks()
// 清空：展厅作品
// 清空localStorage：jademirror-works-v1
```

#### 3. 助手数据（新增）
```javascript
assistantStore.messages = []  // 清空对话历史
assistantStore.memories = []  // 清空记忆
assistantStore.lastMemoryDigest = ''  // 清空记忆摘要
assistantStore.guidedTestActive = false  // 重置测试状态
assistantStore.guidedQuestionIndex = 0  // 重置题目索引
assistantStore.galleryTourAuto = false  // 停止展厅导览
assistantStore.teardown()  // 清理所有定时器
```

## 清空流程

```
用户点击登出
  ↓
调用 authStore.logout()
  ↓
1. 发送登出请求到后端（如果有token）
  ↓
2. 清空认证信息
   - currentUser = null
   - token = ''
   - localStorage清空认证数据
  ↓
3. 清空用户数据
   - 调用 userStore.resetTest()
   - 清空 works 数组
   - localStorage清空作品数据
  ↓
4. 清空助手数据
   - 清空对话历史
   - 清空记忆
   - 重置测试状态
   - 停止所有定时器
  ↓
5. 打印确认日志
  ↓
✅ 所有数据已清空，用户可以重新登录
```

## 为什么使用动态import

```javascript
const { useUserStore } = await import('./userStore')
const { useAssistantStore } = await import('./assistantStore')
```

**原因**：
1. **避免循环依赖**：authStore可能被userStore或assistantStore导入，直接import会造成循环依赖
2. **延迟加载**：只在需要时才加载这些store
3. **确保store已初始化**：动态import确保pinia已经初始化

## 测试验证

### 测试步骤

1. **登录并创建数据**
   ```
   - 登录账号
   - 完成照心测试
   - 生成专属玉
   - 保存到展厅
   - 与玉灵童子对话
   ```

2. **检查数据存在**
   ```
   - 打开浏览器开发者工具
   - 查看Application > Local Storage
   - 应该看到：
     * jademirror-auth-user-v1
     * jademirror-auth-token-v1
     * jademirror-works-v1
   - 查看Vuex/Pinia状态
   - 应该看到测试答案、匹配结果、对话历史等
   ```

3. **登出**
   ```
   - 点击登出按钮
   - 查看控制台，应该显示：
     "✅ 用户登出，所有数据已清空"
   ```

4. **验证数据已清空**
   ```
   - 查看Local Storage
   - 应该看到：
     * jademirror-auth-user-v1: null
     * jademirror-auth-token-v1: ""
     * jademirror-works-v1: []
   - 查看Vuex/Pinia状态
   - 所有数据应该恢复到初始状态
   ```

5. **重新登录**
   ```
   - 重新登录
   - 应该是全新的状态
   - 没有之前的测试答案
   - 没有之前的匹配结果
   - 没有之前的对话历史
   - 展厅是空的
   ```

## 预期效果

### 登出前
```javascript
// authStore
currentUser: { id: 1, username: "user1", ... }
token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

// userStore
testAnswers: { q1: "A", q2: "B", ... }
matchedJade: { name: "龙凤纹玉璧", ... }
generatedImageDataUrl: "data:image/png;base64,..."
works: [{ id: "work-1", ... }, { id: "work-2", ... }]

// assistantStore
messages: [
  { role: "user", content: "开始测试" },
  { role: "assistant", content: "好嘞！..." },
  ...
]
memories: [
  { id: 1, text: "用户喜欢古玉文化", ... },
  ...
]
```

### 登出后
```javascript
// authStore
currentUser: null
token: ""

// userStore
testAnswers: {}
matchedJade: null
generatedImageDataUrl: ""
works: []

// assistantStore
messages: []
memories: []
guidedTestActive: false
guidedQuestionIndex: 0
```

### localStorage
```
登出前：
jademirror-auth-user-v1: {"id":1,"username":"user1",...}
jademirror-auth-token-v1: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
jademirror-works-v1: [{"id":"work-1",...},{"id":"work-2",...}]

登出后：
jademirror-auth-user-v1: null
jademirror-auth-token-v1: ""
jademirror-works-v1: []
```

## 注意事项

### 1. 游客登录
如果用户是游客登录（guestLogin），登出后也会清空所有数据。这是预期行为，因为游客数据不应该持久化。

### 2. 后端记忆
前端清空了memories，但后端数据库中的记忆不会被清空。如果需要清空后端记忆，需要在logout时调用后端的清空记忆API。

### 3. 多标签页
如果用户在多个标签页中打开了应用，在一个标签页登出后，其他标签页的localStorage会被清空，但Pinia状态不会自动更新。建议刷新其他标签页。

### 4. 网络错误
如果登出请求失败（网络错误），前端仍然会清空所有数据。这是安全的做法，确保用户数据不会残留。

## 修改的文件

1. **e:\Jade\jademirror\frontend\src\stores\authStore.js**
   - 修改logout方法，添加清空用户数据和助手数据的逻辑

2. **e:\Jade\登出清空数据功能.md**（本文件）
   - 详细的功能说明文档

## 总结

本次修改确保了用户登出时，所有相关数据都会被彻底清空：

- ✅ 认证信息（用户、token）
- ✅ 测试数据（答案、匹配结果）
- ✅ 生成数据（图片、提示词）
- ✅ 展厅作品
- ✅ 对话历史
- ✅ 长期记忆
- ✅ 测试状态
- ✅ 定时器和导览状态
- ✅ localStorage中的持久化数据

这样可以确保：
1. 用户隐私得到保护
2. 下次登录是全新的状态
3. 不会有数据残留
4. 游客数据不会持久化

---

**修复完成时间**：2026年5月7日  
**修复人员**：Kiro AI Assistant  
**测试状态**：待用户测试验证
