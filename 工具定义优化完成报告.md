# 工具定义优化完成报告

## 优化时间
2026年5月7日

## 优化目标
根据用户建议，为每个工具添加详细的注释和使用场景说明，让AI更清楚地知道：
1. 每个工具是干什么的
2. 每个工具跳转到什么页面
3. 每个页面是干什么的
4. 什么时候应该调用这个工具

## 优化内容

### 1. 详细的工具说明结构

为每个工具添加了以下信息：
- **作用**：工具的功能描述
- **参数**：需要传递的参数及其说明
- **页面说明**（如果涉及跳转）：目标页面的功能
- **前置条件**（如果有）：调用前需要满足的条件
- **何时调用**：具体的使用场景
- **示例**：实际的用户输入和对应的工具调用

### 2. 10个工具的完整说明

#### 工具1：navigate - 页面跳转工具
```
作用：跳转到指定页面
参数：{route: "/test|/result|/chat|/generate|/gallery|/home"}

页面说明：
  • /home - 首页，介绍应用功能
  • /test - 照心测试页，用户在这里回答性格测试题
  • /result - 结果页，展示匹配的古玉和性格分析
  • /chat - 对话页，与匹配的古玉进行深度对话
  • /generate - 生成页，生成用户的专属玉图像
  • /gallery - 展厅页，展示用户收藏的所有专属玉作品

何时调用：
  • 用户明确说"去测试""去展厅""回首页"等
  • 需要配合其他操作时（如开始测试前先跳转到/test）

示例：
  • "去测试" → [{"name": "navigate", "args": {"route": "/test"}}]
  • "去展厅看看" → [{"name": "navigate", "args": {"route": "/gallery"}}]
  • "回首页" → [{"name": "navigate", "args": {"route": "/home"}}]
```

#### 工具2：start_guided_test - 开始引导测试
```
作用：启动AI语音引导的照心测试，你会逐题播报问题并听取用户答案
参数：无
前置条件：用户必须已经选择了测试版本（六问版或完整版）

何时调用：
  • 用户说"开始测试"且已选择版本
  • 通常需要先调用 navigate 跳转到 /test 页面

⚠️ 重要：如果用户只说"开始测试"没有指定版本，必须先用 ask_clarification 询问版本

示例：
  • 用户："开始测试" → [{"name": "ask_clarification", "args": {"question": "你想选六问快速版还是完整深度版？"}}]
  • 用户："六问版" → [{"name": "navigate", "args": {"route": "/test"}}, {"name": "start_guided_test", "args": {}}]
```

#### 工具3：finish_test - 完成测试
```
作用：结束测试，计算匹配结果，展示匹配的古玉
参数：无

何时调用：
  • 所有测试题目已回答完毕
  • 用户说"完成测试""看结果"

示例：
  • "看结果" → [{"name": "finish_test", "args": {}}]
```

#### 工具4：generate_jade - 生成专属玉
```
作用：根据测试结果和用户情绪，使用AI生成用户的专属玉图像
参数：无
前置条件：必须已完成测试并有匹配结果

何时调用：
  • 用户说"生成玉""生成我的玉""生成图片"
  • 如果未完成测试，先提示用户完成测试

示例：
  • "生成我的玉" → [{"name": "generate_jade", "args": {}}]
  • 未完成测试时："生成玉" → [{"name": "ask_clarification", "args": {"question": "要生成专属玉，需要先完成照心测试哦。要开始测试吗？"}}]
```

#### 工具5：save_to_gallery - 保存到展厅
```
作用：将当前生成的专属玉图像保存到用户的个人展厅
参数：无
前置条件：必须已生成图片

何时调用：
  • 用户说"保存""保存到展厅""收藏"
  • 如果未生成图片，先提示用户生成

示例：
  • "保存到展厅" → [{"name": "save_to_gallery", "args": {}}]
```

#### 工具6：start_gallery_tour - 开始展厅导览
```
作用：启动展厅语音导览，你会逐件介绍用户收藏的作品
参数：无

何时调用：
  • 用户说"导览""介绍展厅""讲解作品"

示例：
  • "导览展厅" → [{"name": "start_gallery_tour", "args": {}}]
```

#### 工具7：delete_gallery_work - 删除展厅作品
```
作用：删除展厅中的某件作品
参数：{index: 作品序号从1开始}

何时调用：
  • 用户说"删除第X件""删除作品"

示例：
  • "删除第2件" → [{"name": "delete_gallery_work", "args": {"index": 2}}]
```

#### 工具8：open_gallery_work - 查看展厅作品
```
作用：详细介绍展厅中的某件作品
参数：{index: 作品序号从1开始}

何时调用：
  • 用户说"看第X件""介绍第X个"

示例：
  • "看第1件" → [{"name": "open_gallery_work", "args": {"index": 1}}]
```

#### 工具9：ask_clarification - 询问澄清
```
作用：当用户意图不明确时，向用户询问更多信息
参数：{question: "要问用户的问题"}

何时调用：
  • 用户的请求模糊不清
  • 需要用户做选择（如测试版本）
  • 缺少必要信息无法执行操作

示例：
  • 用户："测试" → [{"name": "ask_clarification", "args": {"question": "你是想开始照心测试吗？"}}]
  • 用户："开始测试" → [{"name": "ask_clarification", "args": {"question": "你想选六问快速版还是完整深度版？"}}]
```

#### 工具10：不调用工具（返回空数组）
```
何时使用：
  • 用户在闲聊玉文化知识
  • 用户问问题（如"为什么古人喜欢玉"）
  • 纯粹的对话交流，不需要执行任何操作

示例：
  • "为什么古人喜欢玉？" → tool_calls: []
  • "你好" → tool_calls: []
```

### 3. 增强的示例部分

从3个示例扩展到6个完整示例，覆盖更多场景：

1. **示例1**：用户说"开始测试"（需要询问版本）
2. **示例2**：用户选择"六问快速版"（跳转并开始）
3. **示例3**：用户说"为什么古人喜欢玉"（闲聊）
4. **示例4**：用户说"去展厅"（跳转页面）
5. **示例5**：用户说"生成我的玉"但未完成测试（需要提示）
6. **示例6**：用户说"导览展厅"（开始导览）

每个示例都包含：
- 用户输入
- 意图分析
- 完整的JSON输出（包含reply、tool_calls、memory、emotion）

## 优化效果

### 优化前
```
【可用工具】
1. navigate - 跳转页面，参数: {route: "/test|/result|/chat|/generate|/gallery|/home"}
2. start_guided_test - 开始测试，无参数
   ⚠️ 重要：调用前必须确认用户选择了测试版本（六问版或完整版）
   如果用户只说"开始测试"没有指定版本，必须先用 ask_clarification 询问
3. finish_test - 完成测试，无参数
...
```

### 优化后
```
【可用工具详解】

1. navigate - 页面跳转工具
   作用：跳转到指定页面
   参数：{route: "/test|/result|/chat|/generate|/gallery|/home"}
   页面说明：
     • /home - 首页，介绍应用功能
     • /test - 照心测试页，用户在这里回答性格测试题
     • /result - 结果页，展示匹配的古玉和性格分析
     • /chat - 对话页，与匹配的古玉进行深度对话
     • /generate - 生成页，生成用户的专属玉图像
     • /gallery - 展厅页，展示用户收藏的所有专属玉作品
   何时调用：
     • 用户明确说"去测试""去展厅""回首页"等
     • 需要配合其他操作时（如开始测试前先跳转到/test）
   示例：
     • "去测试" → [{"name": "navigate", "args": {"route": "/test"}}]
     • "去展厅看看" → [{"name": "navigate", "args": {"route": "/gallery"}}]
     • "回首页" → [{"name": "navigate", "args": {"route": "/home"}}]
...
```

## 关键改进点

### 1. 页面功能说明
明确说明了每个页面的作用：
- `/home` - 首页，介绍应用功能
- `/test` - 照心测试页，用户在这里回答性格测试题
- `/result` - 结果页，展示匹配的古玉和性格分析
- `/chat` - 对话页，与匹配的古玉进行深度对话
- `/generate` - 生成页，生成用户的专属玉图像
- `/gallery` - 展厅页，展示用户收藏的所有专属玉作品

### 2. 前置条件明确
对于有前置条件的工具，明确说明：
- `start_guided_test` - 需要先选择测试版本
- `generate_jade` - 需要先完成测试
- `save_to_gallery` - 需要先生成图片

### 3. 使用场景详细
每个工具都有"何时调用"部分，列出具体的触发场景

### 4. 示例丰富
每个工具都有实际的用户输入和对应的工具调用示例

### 5. 特殊情况处理
明确说明了特殊情况的处理方式：
- 用户说"开始测试"但没选版本 → 先询问
- 用户说"生成玉"但没完成测试 → 先提示
- 用户说"保存"但没生成图片 → 先提示

## 预期效果

### AI决策更准确
- ✅ AI知道每个页面是干什么的
- ✅ AI知道什么时候应该跳转
- ✅ AI知道什么时候需要询问
- ✅ AI知道什么时候只需要聊天

### 用户体验更流畅
- ✅ 用户说"去测试"，AI立即跳转
- ✅ 用户说"开始测试"，AI先询问版本
- ✅ 用户说"六问版"，AI跳转并开始
- ✅ 用户闲聊时，AI不会乱跳转

### 错误处理更友好
- ✅ 未完成测试就想生成 → AI友好提示
- ✅ 未生成图片就想保存 → AI友好提示
- ✅ 意图不明确 → AI主动询问

## 系统提示词结构

优化后的系统提示词结构：

```
【重要】你必须始终返回纯JSON格式，不要有任何JSON之外的文本！

你是"玉灵童子"，一个从古玉里蹦出来的小精灵...

核心人设：
- 说话像朋友聊天...
- 不要用"吾""汝""阁下"...
...

你的目标：
1) 像朋友一样引导用户完成：测试→匹配→对话→生成→展厅...
...

【工具调用决策流程】
第1步：理解用户意图
第2步：检查前置条件
第3步：返回工具调用

【可用工具详解】
1. navigate - 页面跳转工具
   作用：...
   参数：...
   页面说明：...
   何时调用：...
   示例：...

2. start_guided_test - 开始引导测试
   作用：...
   参数：...
   前置条件：...
   何时调用：...
   示例：...

... (共10个工具)

【重要规则】
- 用户意图明确 → 直接调用对应工具
- 用户意图不明确 → 使用 ask_clarification
- 纯闲聊玉文化 → tool_calls 返回空数组 []
...

【输出格式】
输出必须是纯JSON，不要有任何JSON之外的文本：
{
  "reply": "...",
  "tool_calls": [...],
  "memory": [...],
  "emotion": "..."
}

【完整示例】
示例1：用户说"开始测试"（需要询问版本）
用户: "开始测试"
分析：用户想测试，但没说版本 → 需要询问
输出: {...}

示例2：用户选择"六问快速版"（跳转并开始）
用户: "六问快速版"
分析：用户已选择版本 → 跳转到测试页 + 开始测试
输出: {...}

... (共6个完整示例)

【再次强调】
1. 你的输出必须是纯JSON，不要有任何其他文本
2. tool_calls 字段必须存在，即使是空数组 []
3. 闲聊时 tool_calls 必须是空数组 []，不要省略这个字段
4. 需要操作时 tool_calls 必须包含具体的工具调用
5. 每个工具调用必须有 name 和 args 两个字段
```

## 修改的文件

1. **e:\Jade\jademirror\backend\jademirror_core\application.py**
   - 扩展了【可用工具】部分为【可用工具详解】
   - 为每个工具添加了详细的说明结构
   - 增加了页面功能说明
   - 扩展了示例从3个到6个
   - 每个示例都包含意图分析

## 测试建议

### 测试场景1：页面跳转
```
用户："去测试"
预期：AI回复 + 跳转到 /test 页面
预期tool_calls: [{"name": "navigate", "args": {"route": "/test"}}]
```

### 测试场景2：开始测试（需要询问版本）
```
用户："开始测试"
预期：AI询问版本选择
预期tool_calls: [{"name": "ask_clarification", "args": {"question": "..."}}]
```

### 测试场景3：选择版本后开始
```
用户："六问快速版"
预期：AI回复 + 跳转到 /test + 开始测试
预期tool_calls: [
  {"name": "navigate", "args": {"route": "/test"}},
  {"name": "start_guided_test", "args": {}}
]
```

### 测试场景4：闲聊
```
用户："为什么古人喜欢玉？"
预期：AI回复玉文化知识 + 不跳转
预期tool_calls: []
```

### 测试场景5：前置条件不满足
```
用户："生成我的玉"（未完成测试）
预期：AI提示需要先完成测试 + 询问是否开始
预期tool_calls: [{"name": "ask_clarification", "args": {"question": "..."}}]
```

## 总结

本次优化大幅提升了系统提示词的清晰度和可用性：

1. **结构化说明**：每个工具都有统一的说明结构（作用、参数、页面说明、前置条件、何时调用、示例）
2. **页面功能明确**：AI清楚知道每个页面是干什么的
3. **使用场景详细**：AI知道什么时候应该调用什么工具
4. **示例丰富**：6个完整示例覆盖主要使用场景
5. **特殊情况处理**：明确说明了前置条件和错误处理

这些改进应该能够显著提高AI的决策准确性，让工具调用更加可靠和符合预期。

---

**优化完成时间**：2026年5月7日  
**优化人员**：Kiro AI Assistant  
**测试状态**：待用户测试验证
