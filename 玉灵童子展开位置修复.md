# 玉灵童子展开位置修复

## 问题描述

用户反馈：点击小动物展开面板后，面板超出屏幕外，看不到内容。

## 问题原因

**定位冲突：**
- 小动物使用 `left` 和 `top` 定位（可拖拽到任意位置）
- 面板宽度 420px
- 当小动物在右下角时，展开后面板会超出屏幕右侧

**示例：**
```
小动物位置: left: 1820px (屏幕宽度 1920px)
面板宽度: 420px
面板右边缘: 1820 + 420 = 2240px (超出屏幕！)
```

## 解决方案

### 方案：展开时切换定位方式

**收起状态（小动物）：**
- 使用 `left` 和 `top` 定位
- 可以拖到任意位置
- 尺寸：80x80px

**展开状态（面板）：**
- 自动切换到 `right` 和 `bottom` 定位
- 固定在右下角
- 宽度：420px（或屏幕宽度-2rem）

### 技术实现

**CSS 修改：**

```css
.companion {
  position: fixed;
  z-index: 999;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.companion.expanded {
  width: min(420px, calc(100vw - 2rem));
  /* 展开时自动调整位置，确保不超出屏幕 */
  right: 1rem;
  bottom: 1rem;
  left: auto !important;  /* 覆盖内联样式 */
  top: auto !important;   /* 覆盖内联样式 */
}
```

**工作原理：**

1. **收起时**
   ```html
   <aside 
     class="companion"
     style="left: 1820px; top: 920px"
   >
     <!-- 小动物 80x80px -->
   </aside>
   ```

2. **展开时**
   ```html
   <aside 
     class="companion expanded"
     style="left: 1820px; top: 920px"  <!-- 被CSS覆盖 -->
   >
     <!-- 面板 420px宽，固定在右下角 -->
   </aside>
   ```
   
   CSS 的 `!important` 覆盖内联样式：
   - `left: auto !important` 覆盖 `left: 1820px`
   - `top: auto !important` 覆盖 `top: 920px`
   - 使用 `right: 1rem; bottom: 1rem` 定位

## 效果对比

### 修复前

```
屏幕边界
│
│  小动物 (1820, 920)
│    ◠ ◠
│   ┌───┐
│   │• •│
│   │ ω │
│   └───┘
│
│  点击展开 ↓
│
│  ┌─────────────────────────┐
│  │ 玉灵童子          [✕]  │
│  │ ...                     │  → 超出屏幕！
│  └─────────────────────────┘
```

### 修复后

```
屏幕边界
│
│  小动物 (1820, 920)
│    ◠ ◠
│   ┌───┐
│   │• •│
│   │ ω │
│   └───┘
│
│  点击展开 ↓
│
│              ┌─────────────────────────┐
│              │ 玉灵童子          [✕]  │
│              │ ...                     │
│              └─────────────────────────┘
│                                         │
└─────────────────────────────────────────┘
  自动调整到右下角，不超出屏幕
```

## 用户体验

### 1. 小动物可以随意拖动

用户可以将小动物拖到屏幕任意位置：
- 左上角
- 右上角
- 左下角
- 右下角
- 屏幕中间

### 2. 展开面板始终可见

无论小动物在哪里，点击展开后：
- 面板自动出现在右下角
- 完全可见，不超出屏幕
- 宽度自适应（最大420px）

### 3. 收起后回到原位

点击关闭按钮后：
- 面板消失
- 小动物回到之前拖动的位置
- 保持用户的自定义位置

## 交互流程

```
1. 小动物在任意位置 (x, y)
   ↓
2. 用户点击小动物
   ↓
3. 面板展开，自动定位到右下角
   ↓
4. 用户与面板交互
   ↓
5. 用户点击关闭按钮
   ↓
6. 面板收起，小动物回到 (x, y)
```

## 移动端适配

在移动端，面板宽度自动调整：

```css
.companion.expanded {
  width: calc(100vw - 2rem);  /* 屏幕宽度 - 左右边距 */
  left: 1rem !important;
  right: 1rem;
}
```

## 测试建议

### 测试步骤

1. **拖动小动物到不同位置**
   - 拖到左上角，点击展开
   - 拖到右上角，点击展开
   - 拖到左下角，点击展开
   - 拖到右下角，点击展开
   - 拖到屏幕中间，点击展开

2. **验证面板位置**
   - 每次展开，面板都应该在右下角
   - 面板完全可见，不超出屏幕
   - 面板宽度合适

3. **验证收起后位置**
   - 点击关闭按钮
   - 小动物应该回到之前拖动的位置
   - 位置保持不变

### 测试不同屏幕尺寸

1. **大屏幕（1920x1080）**
   - 面板宽度：420px
   - 位置：右下角

2. **中等屏幕（1366x768）**
   - 面板宽度：420px
   - 位置：右下角

3. **小屏幕（手机）**
   - 面板宽度：屏幕宽度 - 2rem
   - 位置：底部，左右留边距

## 相关文件

- `e:\Jade\jademirror\frontend\src\components\CompanionPanel.vue` - 组件实现

## 完成时间

2026年5月7日

## 状态

✅ 已修复

## 总结

通过CSS定位切换解决了展开位置问题：

1. ✅ 收起时使用 `left/top` 定位，可以拖到任意位置
2. ✅ 展开时自动切换到 `right/bottom` 定位，固定在右下角
3. ✅ 使用 `!important` 覆盖内联样式
4. ✅ 面板始终完全可见，不超出屏幕
5. ✅ 收起后小动物回到原位

现在无论小动物在哪里，展开的面板都会正确显示在右下角！
