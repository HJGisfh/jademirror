# 测试答案记录工具 (record_answer)

## 添加时间
2026年5月7日

## 需求描述
用户要求：**应该再写一个选择题目的工具，用户选择之后，AI自动choose答案然后下一题**

## 问题分析

### 当前流程的问题
```
用户: "A"
  ↓
AI: "A选项！雕工巧夺天工...选得好，咱们继续～"
  ↓
AI: "好嘞！第2题来啦～听好了：..."
  ↓
❌ 问题：AI直接播报了下一题，但没有记录答案
❌ 结果：答案没有保存，测试无法完成
```

### 期望的流程
```
用户: "A"
  ↓
AI: "A选项！雕工巧夺天工...选得好，咱们继续～"
tool_calls: [{"name": "record_answer", "args": {"answer": "A"}}]
  ↓
前端: 记录答案 → 自动播报下一题
  ↓
✅ 答案已保存，自动进入下一题
```

## 解决方案

### 1. 添加record_answer工具（后端）

**修改位置**：`e:\Jade\jademirror\backend\jademirror_core\application.py`

**工具定义**：
```python
'4. record_answer - 记录测试答案\n'
'   作用：记录用户对当前题目的答案，并自动进入下一题\n'
'   参数：{answer: "A|B|C|D"}\n'
'   何时调用：\n'
'     • 用户在测试过程中回答了选项（A、B、C、D）\n'
'     • 必须在引导测试激活时调用\n'
'   ⚠️ 重要：\n'
'     • answer参数必须是 "A"、"B"、"C" 或 "D"\n'
'     • 调用后前端会自动记录答案并播报下一题\n'
'     • 你的reply应该简短确认用户的选择，如"A选项！雕工巧夺天工...选得好，咱们继续～"\n'
'     • 不要在reply中播报下一题，前端会自动处理\n'
'   示例：\n'
'     • 用户："A" → reply: "A选项！雕工巧夺天工，看来你是个细节控！选得好，咱们继续～", tool_calls: [{"name": "record_answer", "args": {"answer": "A"}}]\n'
'     • 用户："选B" → reply: "B！造型奇特诡异，你喜欢独特的东西～继续下一题！", tool_calls: [{"name": "record_answer", "args": {"answer": "B"}}]\n'
```

**添加示例7**：
```python
'示例7：用户在测试中回答"A"（记录答案）\n'
'用户: "A"\n'
'分析：用户在测试中选择了A选项 → 记录答案（前端会自动播报下一题）\n'
'输出: {\n'
'  "reply": "A选项！雕工巧夺天工，看来你是个细节控！选得好，咱们继续～",\n'
'  "tool_calls": [\n'
'    {"name": "record_answer", "args": {"answer": "A"}}\n'
'  ],\n'
'  "memory": []\n'
'}\n'
```

### 2. 实现record_answer工具（前端）

**修改位置**：`e:\Jade\jademirror\frontend\src\stores\assistantStore.js`

**executeSingleTool中添加**：
```javascript
case 'record_answer':
  console.log(`📝 记录测试答案`)
  const answer = String(args.answer || '').toUpperCase()
  console.log(`   - 答案: ${answer}`)
  
  if (!this.guidedTestActive) {
    console.warn(`   - ⚠️ 测试未激活，无法记录答案`)
    break
  }
  
  const currentQ = quickTestQuestions[this.guidedQuestionIndex]
  if (!currentQ) {
    console.warn(`   - ⚠️ 当前没有题目`)
    break
  }
  
  console.log(`   - 当前题目: ${currentQ.id}`)
  
  // 记录答案
  const userStore2 = useUserStore()
  userStore2.setAnswer(currentQ.id, answer)
  console.log(`   - ✅ 答案已记录: ${currentQ.id} = ${answer}`)
  
  // 进入下一题
  this.guidedQuestionIndex += 1
  console.log(`   - 当前题目索引: ${this.guidedQuestionIndex}`)
  
  // 检查是否完成所有题目
  if (this.guidedQuestionIndex >= quickTestQuestions.length) {
    console.log(`   - 🎉 所有题目已完成！`)
  } else {
    console.log(`   - 准备播报下一题`)
  }
  break
```

### 3. 自动播报下一题（前端）

**修改位置**：`assistantStore.js` 的 `sendTurn` 函数

```javascript
// 检查是否刚刚记录了答案
const hasRecordAnswer = data.tool_calls.some(call => 
  call.name === 'record_answer'
)

// 如果刚记录了答案，自动播报下一题
if (hasRecordAnswer && this.guidedTestActive) {
  console.log('📝 答案已记录，准备播报下一题')
  
  // 检查是否还有下一题
  if (this.guidedQuestionIndex < quickTestQuestions.length) {
    console.log(`   - 下一题索引: ${this.guidedQuestionIndex}`)
    await new Promise(resolve => setTimeout(resolve, 300))
    
    const nextQ = quickTestQuestions[this.guidedQuestionIndex]
    const qCtx = this.buildAgentContext()
    const qData = await requestAssistantTurn({
      text: `[系统指令：请用自然语言播报第${this.guidedQuestionIndex + 1}题，题目数据见 context.test.questions[${this.guidedQuestionIndex}]]`,
      stage: 'test',
      context: qCtx,
    })
    const qReply = qData.reply || this.legacyBuildQuestionGuide(nextQ, this.guidedQuestionIndex)
    this.appendMessage('assistant', qReply)
    this.speak(qReply)
  } else {
    console.log('   - 🎉 所有题目已完成！')
    // 提示用户可以查看结果
    const finishMsg = '太棒了！所有题目都答完啦～想看看你匹配到哪件古玉吗？说"看结果"我就帮你算～'
    this.appendMessage('assistant', finishMsg)
    this.speak(finishMsg)
  }
}
```

## 完整流程

### 场景1：正常答题流程

```
用户: "六问快速版"
  ↓
AI: "好嘞！六问版走起～那咱们就正式开始啦！"
tool_calls: [{"name": "start_guided_test", "args": {"mode": "quick"}}]
  ↓
前端: 设置testMode='quick' → 激活测试 → 自动播报第1题
  ↓
AI: "好嘞！第1题来啦～听好了：在博物馆，你被一件古器物深深吸引，通常是因为它——A雕工巧夺天工，B造型奇特诡异，C重大历史的见证，还是D线条流畅柔美？选一个你最有感觉的！"
  ↓
用户: "A"
  ↓
AI: "A选项！雕工巧夺天工，看来你是个细节控！选得好，咱们继续～"
tool_calls: [{"name": "record_answer", "args": {"answer": "A"}}]
  ↓
前端: 记录答案 q1=A → guidedQuestionIndex++ → 自动播报第2题
  ↓
AI: "好嘞！第2题来啦～听好了：面对人际交往中不可避免的冲突，你的本能反应是？A像刀锋划清界限，B像水一样包容化解，C像旁观者抽离，还是D像火一样直接表达？选一个最像你的！"
  ↓
用户: "B"
  ↓
AI: "B！像水一样包容化解，你是个和平主义者～继续！"
tool_calls: [{"name": "record_answer", "args": {"answer": "B"}}]
  ↓
前端: 记录答案 q2=B → guidedQuestionIndex++ → 自动播报第3题
  ↓
... (继续直到第6题)
  ↓
用户: "D"（第6题的答案）
  ↓
AI: "D！选得好～"
tool_calls: [{"name": "record_answer", "args": {"answer": "D"}}]
  ↓
前端: 记录答案 q6=D → guidedQuestionIndex++ → 检测到所有题目完成
  ↓
AI: "太棒了！所有题目都答完啦～想看看你匹配到哪件古玉吗？说"看结果"我就帮你算～"
  ↓
用户: "看结果"
  ↓
AI: "好嘞！马上给你算～"
tool_calls: [{"name": "finish_test", "args": {}}]
  ↓
前端: 计算匹配结果 → 跳转到结果页 → 展示匹配的古玉
```

### 场景2：用户说"选A"而不是"A"

```
用户: "选A"
  ↓
AI: "A选项！..."
tool_calls: [{"name": "record_answer", "args": {"answer": "A"}}]
  ↓
前端: 提取"A" → 记录答案 → 自动播报下一题
```

### 场景3：用户说"我选第一个"

```
用户: "我选第一个"
  ↓
AI: "第一个是A选项！..."
tool_calls: [{"name": "record_answer", "args": {"answer": "A"}}]
  ↓
前端: 记录答案 → 自动播报下一题
```

## 关键改进

### 1. 职责分离
- **AI的职责**：理解用户选择、给出简短评论、调用record_answer工具
- **前端的职责**：记录答案、管理题目索引、自动播报下一题

### 2. 自动化流程
- 用户只需说答案（A/B/C/D）
- AI自动记录答案
- 前端自动播报下一题
- 无需用户说"下一题"

### 3. 完成提示
- 所有题目完成后，自动提示用户可以查看结果
- 不会自动完成测试，等待用户确认

### 4. 灵活的输入
- 支持"A"、"选A"、"我选A"、"第一个"等多种表达
- AI负责理解并提取正确的选项

## 预期日志输出

当用户说"A"后，浏览器控制台应该显示：

```
================================================================================
📥 前端接收到的数据: {
  "reply": "A选项！雕工巧夺天工，看来你是个细节控！选得好，咱们继续～",
  "tool_calls": [
    {"name": "record_answer", "args": {"answer": "A"}}
  ],
  "memory": []
}
================================================================================
✅ 执行 tool_calls: [...]
🎬 开始执行工具调用列表: [...]

📌 准备执行工具 #1: record_answer
🔧 执行工具: record_answer
   参数: {answer: "A"}
   router存在: true
   router.push存在: true
📝 记录测试答案
   - 答案: A
   - 当前题目: q1
   - ✅ 答案已记录: q1 = A
   - 当前题目索引: 1
   - 准备播报下一题
✅ 所有工具执行完毕

📝 答案已记录，准备播报下一题
   - 下一题索引: 1
（等待300ms后）
📥 前端接收到的数据: {"reply": "好嘞！第2题来啦～...", ...}
```

## 修改的文件

1. **e:\Jade\jademirror\backend\jademirror_core\application.py**
   - 添加record_answer工具定义
   - 添加示例7展示答题流程

2. **e:\Jade\jademirror\frontend\src\stores\assistantStore.js**
   - 在executeSingleTool中添加record_answer case
   - 在sendTurn中添加自动播报下一题的逻辑
   - 添加完成提示

3. **e:\Jade\测试答案记录工具.md**（本文件）
   - 详细的功能说明文档

## 测试验证

### 测试步骤

1. **开始测试**
   ```
   说"开始测试" → 选择"六问快速版"
   ```

2. **回答第1题**
   ```
   说"A"
   预期：AI确认选择 → 自动播报第2题
   ```

3. **回答第2题**
   ```
   说"B"
   预期：AI确认选择 → 自动播报第3题
   ```

4. **继续回答直到第6题**
   ```
   说"D"
   预期：AI确认选择 → 提示"所有题目都答完啦～想看看你匹配到哪件古玉吗？"
   ```

5. **查看结果**
   ```
   说"看结果"
   预期：计算匹配结果 → 跳转到结果页
   ```

### 验证要点

- ✅ 每次回答后，答案是否正确记录到userStore.testAnswers
- ✅ 是否自动进入下一题
- ✅ 是否自动播报下一题
- ✅ 第6题后是否提示完成
- ✅ 说"看结果"后是否正确计算匹配

## 总结

本次添加的record_answer工具实现了：

- ✅ 自动记录用户答案
- ✅ 自动进入下一题
- ✅ 自动播报下一题
- ✅ 完成后自动提示
- ✅ 流畅的答题体验
- ✅ 无需用户说"下一题"

用户只需说答案（A/B/C/D），AI会自动处理一切！

---

**添加完成时间**：2026年5月7日  
**开发人员**：Kiro AI Assistant  
**测试状态**：待用户测试验证
