# 📝 测试流程优化说明

## 问题描述

用户说"开始测试"后，AI直接开始播报题目，但没有：
1. 询问用户选择哪个测试版本（六问版 vs 完整版）
2. 调用工具跳转到测试页面

## 优化内容

### 1. 更新系统提示词 ✅

**文件**: `jademirror/backend/jademirror_core/application.py`

**修改内容**:

#### 工具说明
```
2. start_guided_test - 开始测试，无参数
   ⚠️ 重要：调用前必须确认用户选择了测试版本（六问版或完整版）
   如果用户只说"开始测试"没有指定版本，必须先用 ask_clarification 询问
```

#### 示例更新
```
【示例1：明确意图】
用户: "开始测试"
输出: {
  "reply": "好嘞！咱们有两个版本：六问快速版（5分钟）和完整深度版（15分钟）。你想选哪个？",
  "tool_calls": [
    {"name": "ask_clarification", "args": {"question": "你想选六问快速版还是完整深度版？"}}
  ],
  "memory": [],
  "emotion": "excited"
}

【示例2：用户选择版本后】
用户: "选择六问的"
输出: {
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ],
  "memory": ["用户选择六问版测试"],
  "emotion": "excited"
}
```

---

## 正确的测试流程

### 场景1：用户说"开始测试"

```
第1轮对话：
用户: "开始测试"
    ↓
AI分析: 用户想开始测试，但没有指定版本
    ↓
AI返回: {
  "reply": "好嘞！咱们有两个版本：六问快速版（5分钟）和完整深度版（15分钟）。你想选哪个？",
  "tool_calls": [
    {"name": "ask_clarification", "args": {"question": "你想选六问快速版还是完整深度版？"}}
  ]
}
    ↓
前端执行: ask_clarification（AI询问，不跳转）
    ↓
AI播报: "好嘞！咱们有两个版本：六问快速版（5分钟）和完整深度版（15分钟）。你想选哪个？"
```

```
第2轮对话：
用户: "选择六问的"
    ↓
AI分析: 用户选择了六问版，可以开始测试
    ↓
AI返回: {
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ]
}
    ↓
前端执行:
  1. navigate → 跳转到 /test 页面
  2. start_guided_test → 开始语音引导测试
    ↓
AI播报: "好嘞！六问版走起～那咱们就正式开始啦！"
    ↓
AI播报第一题: "第一题来咯：在博物馆，你被一件古器物深深吸引..."
```

---

### 场景2：用户直接说"开始六问测试"

```
用户: "开始六问测试"
    ↓
AI分析: 用户明确指定了六问版，可以直接开始
    ↓
AI返回: {
  "reply": "好嘞！六问版走起～那咱们就正式开始啦！",
  "tool_calls": [
    {"name": "navigate", "args": {"route": "/test"}},
    {"name": "start_guided_test", "args": {}}
  ]
}
    ↓
前端执行:
  1. navigate → 跳转到 /test 页面
  2. start_guided_test → 开始语音引导测试
    ↓
AI播报第一题
```

---

## 测试版本说明

### 六问快速版 (quickTestQuestions)
- **题目数量**: 6题
- **预计时间**: 5分钟
- **题目ID**: fq1, fq2, fq3, fq4, fq5, fq6
- **适合**: 快速体验

### 完整深度版 (deepTestQuestions)
- **题目数量**: 约20题
- **预计时间**: 15分钟
- **题目ID**: q1, q2, q3, ...
- **适合**: 深度测试

---

## 调试方法

### 1. 查看AI返回的 tool_calls

在浏览器Console中：
```javascript
// 应该看到
{
  reply: "好嘞！咱们有两个版本...",
  tool_calls: [
    {name: "ask_clarification", args: {...}}
  ]
}
```

### 2. 查看是否跳转

第二轮对话后，应该看到：
```javascript
{
  reply: "好嘞！六问版走起～...",
  tool_calls: [
    {name: "navigate", args: {route: "/test"}},
    {name: "start_guided_test", args: {}}
  ]
}
```

### 3. 验证页面跳转

- URL应该从 `/` 变为 `/test`
- 页面应该显示测试界面

---

## 常见问题

### Q1: AI没有询问版本，直接开始测试

**原因**: AI可能没有理解提示词中的要求

**解决**: 
- 检查系统提示词是否正确更新
- 查看AI返回的 tool_calls 是否包含 ask_clarification

### Q2: AI询问了版本，但用户选择后没有跳转

**原因**: AI可能没有返回 navigate 工具调用

**解决**:
- 查看第二轮对话的 tool_calls
- 应该包含 navigate 和 start_guided_test

### Q3: 跳转了但没有播报第一题

**原因**: start_guided_test 可能没有正确执行

**检查**:
```javascript
// assistantStore.js
case 'start_guided_test':
  this.guidedTestActive = true
  this.guidedQuestionIndex = 0
  if (this.autoGuide && router) {
    router.push('/test')
  }
  break
```

---

## 测试用例

### 测试1：完整流程
```
1. 用户: "开始测试"
   期望: AI询问选择版本，不跳转

2. 用户: "选择六问的"
   期望: AI跳转到测试页，开始测试，播报第一题
```

### 测试2：直接指定版本
```
用户: "开始六问测试"
期望: AI直接跳转到测试页，开始测试，播报第一题
```

### 测试3：完整版
```
1. 用户: "开始测试"
   期望: AI询问选择版本

2. 用户: "完整版"
   期望: AI跳转到测试页，开始测试，播报第一题
```

---

## 总结

✅ **已优化**:
- 系统提示词中添加了版本选择要求
- 添加了 ask_clarification 示例
- 添加了用户选择版本后的示例

✅ **预期效果**:
- 用户说"开始测试" → AI询问版本
- 用户选择版本 → AI跳转页面 + 开始测试
- 用户直接说"开始六问测试" → AI直接跳转 + 开始测试

⚠️ **待测试**:
- 实际运行验证AI是否正确询问版本
- 验证用户选择后是否正确跳转
- 验证是否正确播报第一题

---

**更新时间**: 2026-05-07  
**版本**: v2.3 - 测试流程优化
